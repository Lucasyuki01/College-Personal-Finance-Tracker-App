import pandas as pd
import matplotlib.pyplot as plt

def plot_monthly_trend(df: pd.DataFrame) -> None:
    """Generate a line chart of total spending per month."""
    pass

def plot_spending_by_category(df: pd.DataFrame) -> None:
    """Generate a bar chart of spending by category."""
    pass

def plot_spending_distribution(df: pd.DataFrame) -> None:
    """Generate a pie chart showing percentage distribution by category."""
    pass
